import { NotFoundImagePipe } from './not-found-image.pipe';

describe('NotFoundImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NotFoundImagePipe();
    expect(pipe).toBeTruthy();
  });
});
