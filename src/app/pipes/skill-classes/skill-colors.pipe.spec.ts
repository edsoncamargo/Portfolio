import { SkillColorsPipe } from './skill-colors.pipe';

describe('SkillColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
